# ── 301 redirects for removed/renamed URLs ────────────────────────
# Cloudflare Pages reads this file. First match wins, so list the
# specific mappings before the section-wide wildcards.

# Old "writing" blog (section removed) → homepage.
/writing/*                                      /            301
/writing                                        /            301

# "Work" / case studies replaced by the notebook-backed "Projects" section.
/work/*                                         /projects    301
/work                                           /projects    301
